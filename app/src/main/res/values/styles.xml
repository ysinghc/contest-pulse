<resources>

    <!-- Base application theme with dynamic DayNight support -->
    <style name="Theme.ContestPulse" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>

    <!-- Fullscreen Emergency Alarm Theme -->
    <style name="Theme.ContestPulse.LoudAlarm" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/alarm_red_dark</item>
        <item name="android:statusBarColor">@color/alarm_red_dark</item>
        <item name="android:navigationBarColor">@color/alarm_red_dark</item>
    </style>

</resources>
